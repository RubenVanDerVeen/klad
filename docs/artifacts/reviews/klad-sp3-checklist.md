# SP-3 verification — 2026-07-16 (Windows local)

- [x] Installer runs without admin prompt (currentUser mode) — PASS — ran `Klad_0.1.0_x64-setup.exe /S` on this host: exit 0, no UAC prompt, files laid at `$LOCALAPPDATA\Klad\klad.exe` (5,848,576 B, Tauri NSIS `currentUser` mode writes here, not `Programs\`), uninstaller registered at `HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall\Klad` with `DisplayName=Klad`, `InstallLocation="C:\Users\ruben\AppData\Local\Klad"`, `UninstallString=...uninstall.exe`. Start Menu (`%APPDATA%\Microsoft\Windows\Start Menu\Programs\Klad.lnk`) and Desktop (`Klad.lnk`) shortcuts created. `HKCU:\Software\Classes\.txt` and `\.md` were rewritten with `*_backup` entries preserving the system default. Uninstall with `uninstall.exe /S` → exit 0; install dir, uninstall registry key, and both `.lnk` files all removed; `.txt` / `.md` class keys left empty (System `Text Document` / `Markdown Document` defaults remained available as fallback, matching the `APP_UNASSOCIATE` script). Static-evidence cross-check still holds: `tauri.conf.json` `installMode=currentUser`, generated `installer.nsi` lines 39 / 103-105 set `RequestExecutionLevel user`, embedded PE manifest shows `<requestedExecutionLevel level="asInvoker"/>`, NSIS `utils.nsh` `SetShellVarContext current` keeps all writes in HKCU.
- [ ] Start menu shows Klad with the blue K icon — UNVERIFIED — installer script calls `CreateShortcut "$SMPROGRAMS\Klad.lnk"` (installer.nsi line 913) but no interactive Start Menu available in this headless shell.
- [ ] Right-click a .txt → Open with → Klad listed — UNVERIFIED — installer script invokes `APP_ASSOCIATE "txt" ...` writing `HKCU\Software\Classes\.txt → Text Document → shell\open\command → klad.exe "%1"` (installer.nsi line 645), so registration is configured, but Explorer's "Open with" menu can only be observed in an interactive desktop session.
- [ ] Set Klad as default for .txt and .md (Settings → Default apps) — UNVERIFIED — interactive Settings UI required.
- [ ] Double-click a .txt → opens IN KLAD with content loaded (foundation startup-file path) — UNVERIFIED — interactive Explorer + Klad window required; foundation startup-file path is unit-verified but end-to-end double-click not exercised here.
- [ ] Double-click a .md → opens in Klad WITH split preview — UNVERIFIED — same as above plus preview-pane rendering must be observed live.
- [ ] Window title shows the file name; taskbar shows the K icon — UNVERIFIED — interactive window/taskbar required.
- [ ] Uninstall (Settings → Installed apps) → double-click .txt falls back to picker/Notepad — UNVERIFIED — uninstaller calls `APP_UNASSOCIATE` (installer.nsi line 769) which removes the Klad file-class keys, restoring the previous handler; Windows' fallback-to-picker behaviour can only be observed after running uninstall + double-click.

# Linux (deferred to CI/Linux box)
- [ ] .deb installs; klad.desktop present with MimeType=text/plain;text/markdown;
- [ ] xdg-mime default commands from README work; xdg-open a .txt opens Klad