# klad memory index

Cross-session facts. Format reference: https://github.com/RubenVanDerVeen/skills/blob/main/skills/rubens-project-standardization/references/memory.md

## User

Ruben is the single author of klad. Owns this Tauri 2 + vanilla TypeScript + Rust app end-to-end (frontend, backend, build, release). Prefers terse technical communication. Dutch native, English fluent.

## Project: typst-preview (in flight)

Branch `feat/typst-preview`. Canonical artifacts live under `docs/artifacts/features/` (look for the typst single-file preview feature folder). Spec, plan, and report filenames follow `YYYY-MM-DD-<topic>-<type>.md`. No commit/push without explicit instruction; carve-out for plan execution.

ponytail: single-file MEMORY.md instead of `docs/memory/` dir. Two entries fit inline; upgrade to typed topic files when this index exceeds ~10 entries.
